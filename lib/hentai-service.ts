'use client';

/**
 * @fileOverview Serviço de Integração HentaiOcean API - REMOVIDO
 */

export async function fetchHentaiData(slug: string): Promise<any | null> {
  return null;
}

export function getEmbedUrl(slug: string): string {
  return '';
}

export function getCoverUrl(filename: string): string {
  return '';
}

export function getThumbnailUrl(slug: string): string {
  return '';
}
